import 'package:flutter/material.dart';

class Group extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is group tab',
        style: TextStyle(fontSize: 25.0, fontFamily: 'Yanon Light'),
      ),
    );
  }
}
